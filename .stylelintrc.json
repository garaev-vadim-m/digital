{
  "extends": ["stylelint-config-standard", "stylelint-config-prettier"],
  "rules": {
    "indentation": 2,
    "selector-class-pattern": [
      "^[a-z]+(_[a-z]+)*$",
      {
        "message": "Имена классов должны быть в формате snake_case (например, header_listLink)"
      }
    ]
  }
}
